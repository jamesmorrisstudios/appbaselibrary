<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Set primary coloring in the app -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorButtonNormal">@color/primary</item>
        <item name="colorControlNormal">@color/primaryDark</item>
        <!--
        <item name="colorControlNormal">@color/accent</item>
        <item name="colorControlActivated">@color/accent</item>
        <item name="colorControlHighlight">@color/accent</item>
        <item name="colorSwitchThumbNormal">@color/accent</item>
        -->

        <item name="android:textSelectHandle">@drawable/text_select_handle_middle_mtrl</item>
        <item name="android:textSelectHandleLeft">@drawable/text_select_handle_left_mtrl</item>
        <item name="android:textSelectHandleRight">@drawable/text_select_handle_right_mtrl</item>

        <item name="spinnerDropDownItemStyle">@style/CustomDropDownItemStyle</item>

    </style>

    <style name="CustomDropDownItemStyle">
        parent="@android:style/Widget.AppCompat.Dark.DropDownItem.Spinner">
        <item name="android:textAppearance">@style/CustomDropDownItemTextStyle</item>
    </style>

    <style name="CustomDropDownItemTextStyle">
        parent="@android:style/Widget.AppCompat.Spinner.DropDown">
        <item name="android:textColor">@color/textDarkMain</item>
        <item name="android:textSize">18sp</item>
        <item name="android:gravity">center_horizontal</item>
    </style>

    <style name="MyDropDownListView" parent="@style/Widget.AppCompat.ListView.DropDown">
        <item name="android:background">@color/primary</item>
    </style>

    <style name="myToolbar" parent="ThemeOverlay.AppCompat.Light">
        <!-- Title text -->
        <item name="android:textColorPrimary">@android:color/white</item>

        <!-- Title color in AppCompat.Light  -->
        <item name="android:textColorPrimaryInverse">@android:color/white</item>

        <!-- Menu text-->
        <item name="actionMenuTextColor">@android:color/white</item>
        <!-- Overflow -->
        <item name="android:textColorSecondary">@android:color/white</item>
        <!-- This will change drawer icon -->
        <item name="drawerArrowStyle">@style/WhiteDrawerIconStyle</item>
        <!-- background of the  -->
        <!-- <item name="android:background">@color/color_primary</item> -->
    </style>

    <!-- Theme for the toolbars in the app-->
    <style name="ThemeOverlay.toolbar" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:colorBackground">@color/primaryDark</item>
        <item name="android:textColor">@color/textLightMain</item>
    </style>

    <style name="WhiteDrawerIconStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <style name="alertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the buttons -->
        <item name="colorAccent">@color/accent</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">@color/textDarkMain</item>
        <!-- Used for the title and text -->
        <item name="android:textColorSecondary">@color/textDarkSecondary</item>
        <!-- Used for the background -->
        <item name="android:background">@color/primaryLight</item>
    </style>


</resources>